{
  "name": "mail-inbox-demo",
  "version": "1.0.0",
  "description": "",
  "main": "./src/server.js",
  "scripts": {
    "start": "node ./src/server",
    "dev": "nodemon ./src/server",
    "sync": "rm -rf ./src/public && cp -a ../client/build ./src/public"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.0",
    "mongoose": "^6.3.1"
  },
  "devDependencies": {
    "dotenv": "^16.0.0",
    "nodemon": "^2.0.15"
  },
  "nodemonConfig": {
    "env": {
      "NODE_ENV": "development"
    }
  }
}
